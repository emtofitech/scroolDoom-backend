server:
  port: ${PORT:8080}
  address: 0.0.0.0

spring:
  data:
    mongodb:
      uri: ${MONGODB_URI}

api:
  base-path: /api/v1

jwt:
  secret: ${JWT_SECRET:myDefaultSecretKeyThatIsLongEnoughForHS256Algorithm}
  expiration: 86400000
